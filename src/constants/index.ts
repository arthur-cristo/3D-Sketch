export const ZOOM = {
  MIN_ZOOM: 0.1,
  MAX_ZOOM: 2,
  ZOOM_STEP: 0.1,
};

export const GRID = {
  SIZE: 25,
};

export const SNAP_THRESHOLD = 20;
export const RULER_OFFSET = 15;
export const PIXELS_PER_CM = 0.5;